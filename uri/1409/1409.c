#include <stdio.h>


int main ( ) {
  int m, n, g, k;

  scanf("%d %d %d %d", &m, &n, &g, &k);
  while (m != 0 && n != 0 && g != 0 && k != 0 ) {




    scanf("%d %d %d %d", &m, &n, &g, &k);
  }
  printf("yaaaay\n");
  return 0;
}
